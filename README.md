# Portfolio — single-file deploy

One file: index.html (everything inlined).

- Netlify: drag this folder onto netlify.com/drop
- Vercel: `npx vercel --prod` from inside this folder
- GitHub Pages: commit index.html to the repo root, enable Pages
